import { addDoc, collection, doc, serverTimestamp, setDoc } from "firebase/firestore"; import { db } from "@/lib/firebase"; import type { Trip } from "@/lib/types";
export async function ensureProfile(uid:string,name:string,email:string,photoURL:string|null){await setDoc(doc(db,"users",uid),{uid,name,email,photoURL,updatedAt:serverTimestamp(),createdAt:serverTimestamp()},{merge:true});}
export async function createTrip(data:Omit<Trip,"id"|"ownerId"|"status">, owner:{uid:string;name:string;email:string;photoURL:string|null}){const trip=await addDoc(collection(db,"trips"),{...data,ownerId:owner.uid,memberIds:[owner.uid],status:"planning",createdAt:serverTimestamp(),updatedAt:serverTimestamp()});await setDoc(doc(db,"trips",trip.id,"members",owner.uid),{...owner,role:"owner",joinedAt:serverTimestamp()});return trip.id;}
