"use client"; import Link from "next/link"; import { useRouter } from "next/navigation"; import { useAuth } from "@/components/auth/AuthProvider";
export function Header(){const {user,signOut}=useAuth();const r=useRouter();return <header className="mx-auto flex max-w-5xl items-center justify-between px-5 py-5"><Link className="text-xl font-black text-teal-700" href="/dashboard">TripSplit</Link><div className="flex items-center gap-3">{user?.photoURL&&<img className="h-9 w-9 rounded-full" src={user.photoURL} alt=""/>}<button className="text-sm font-semibold" onClick={async()=>{await signOut();r.replace("/")}}>Log out</button></div></header>}
